/* 6. Escreva um programa que receba uma letra representando uma categoria de habilitação (A,
    B, C, D, ou E) e exiba uma mensagem informando para quais tipos de veículos essa
    categoria permite dirigir. Utilize switch para cada caso.*/

    // Definindo variavel prompt
const prompt = require(`prompt-sync`)();

// Pedindo para o usuario digitar uma letra
const letra = (prompt(`Digite uma letra: `));